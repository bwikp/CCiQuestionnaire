{% extends 'base.html.twig' %}

{% block title %}DernierEmploiStage{% endblock %}

{% block body %}
    <h1>DernierEmploiStage</h1>
        {% for item in dernierEmploiStage %}
            
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ item.annee ? item.annee|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ item.duree }}</td>
            </tr>
            <tr>
                <th>Nom_entreprise</th>
                <td>{{ item.nomEntreprise }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ item.ville }}</td>
            </tr>
            <tr>
                <th>Poste_occupe</th>
                <td>{{ item.posteOccupe }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_dernierEmploiStage_index') }}">back to list</a> #}

    {# <a href="{{ path('app_dernierEmploi_stage_edit', {'id': item.id}) }}">edit</a> #}

    {# {{ include('dernierEmploiStage/_delete_form.html.twig') }} #}
        {% endfor %}

{% endblock %}

<form method="post" action="{{ path('app_thematique_delete', {'id': thematique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ thematique.id) }}">
    <button class="btnDelete">Delete</button>
</form>

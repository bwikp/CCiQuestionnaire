{% extends 'base.html.twig' %}

{% block title %}Thematique index{% endblock %}

{% block body %}
    <h1>Thematique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Duree</th>
                <th>Nombre_question</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for thematique in thematiques %}
            <tr>
                <td>{{ thematique.id }}</td>
                <td>{{ thematique.duree }}</td>
                <td>{{ thematique.nombreQuestion }}</td>
                <td>{{ thematique.nom }}</td>
                <td>
                    <a href="{{ path('app_thematique_show', {'id': thematique.id}) }}">show</a>
                    <a href="{{ path('app_thematique_edit', {'id': thematique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_thematique_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}ReponseCandidat{% endblock %}

{% block body %}
    <h1>ReponseCandidat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse_candidat.id }}</td>
            </tr>
            <tr>
                <th>Reponse_candidat</th>
                <td>{{ reponse_candidat.reponseCandidat }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ reponse_candidat.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_candidat_index') }}">back to list</a>

    <a href="{{ path('app_reponse_candidat_edit', {'id': reponse_candidat.id}) }}">edit</a>

    {{ include('reponse_candidat/_delete_form.html.twig') }}
{% endblock %}

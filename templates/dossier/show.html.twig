{% extends 'base.html.twig' %}

{% block title %}Dossier
{% endblock %}

{% block body %}
	<div class="FormationIndex">
		<h1>Dossier:
			{{dossier.candidat.nom}}
			{{dossier.candidat.prenom}}</h1>
		<div class="container">
			<table class="table table-bordered">
				<tbody>
					<tr>
						<th>Id</th>
						<td>{{ dossier.id }}</td>
					</tr>
					<th>Nom</th>
					<td>{{ dossier.candidat.nom }}</td>
				</tbody>
			</tr>
			<tr>
				<th>Prenom</th>
				<td>{{ dossier.candidat.prenom }}</td>
			</tr>
			<tr>
				<th>genre</th>
				<td>{{ dossier.candidat.genre }}</td>
			</tr>
			<tr>
				<th>Motivation</th>
				<td>{{ dossier.motivation.projetProEtMotivation }}</td>
			</tr>
			<tr>
				<th>Experience Pro</th>
				<td>{{ dossier.experiencePro }}</td>
			</tr>
			<tr>
				<th>motivation</th>
				<td>
					<a href="{{ path('app_motivation_show', {'id': dossier.motivation.id}) }}">motivation</a>
				</td>
			</tr>
			<th>Experience_pro</th>
			<td>{{ dossier.experiencePro }}</td>
		</tr>
	</tbody>
</table>
<a href="{{ path('app_dossier_index') }}">back to list</a>

<a href="{{ path('app_dossier_edit', {'id': dossier.id}) }}">edit</a>

{{ include('dossier/_delete_form.html.twig') }}</div></div>{% endblock %}

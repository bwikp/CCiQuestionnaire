{% extends 'base.html.twig' %}

{% block title %}QuestionCorriger{% endblock %}

{% block body %}
    <h1>QuestionCorriger</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_corriger.id }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ question_corriger.detail }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ question_corriger.reponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_question_corriger_index') }}">back to list</a>

    <a href="{{ path('app_question_corriger_edit', {'id': question_corriger.id}) }}">edit</a>

    {{ include('question_corriger/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}QuestionQcm index{% endblock %}

{% block body %}
    <h1>QuestionQcm index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Detail</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for question_qcm in question_qcms %}
            <tr>
                <td>{{ question_qcm.id }}</td>
                <td>{{ question_qcm.detail }}</td>
                <td>
                    <a href="{{ path('app_question_qcm_show', {'id': question_qcm.id}) }}">show</a>
                    <a href="{{ path('app_question_qcm_edit', {'id': question_qcm.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_question_qcm_new') }}">Create new</a>
{% endblock %}
